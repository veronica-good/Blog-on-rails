<ul class="nav navbar navbar-expand-lg navbar-light bg-light mb-3">
        
    <div class="nav-item">
    <%=link_to 'Blog On Rails', root_path, class: 'nav-link' %>
    </div>
    <div class=" d-flex justify-content-end">
        <div class="nav-item">
            <%=link_to 'Home', root_path, class: 'nav-link'%>
        </div>

        <div class="nav-item">
            <%=link_to 'New Post', new_post_path, class: 'nav-link'%>
        </div>

        <% if user_signed_in? %>  
            <div class="nav-item">    
                <em class="m-1">Hello, <%= current_user.name %></em>  
           </div>
           <div class="nav-item">
                <%= link_to "Logout", session_path, method: :delete,
                data: {confirm: 'Are you sure you want to log out?'}%>
            </div>
        <% else %>
            <%= link_to "Login", new_session_path, class: 'nav-item btn btn-primary m-2' %>
            <%= link_to "Sign Up", new_user_path, class: 'nav-item btn btn-primary m-2' %>
        <% end %>
    </div>
</ul>